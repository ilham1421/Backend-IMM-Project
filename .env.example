# Database
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@127.0.0.1:5432/imm_perkaderan?schema=public"

# JWT (generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=""

# Server
PORT=5000
FRONTEND_URL="http://localhost:3000"

# SMTP (opsional, untuk email notifikasi)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
MAIL_FROM_NAME="Perkaderan IMM"
